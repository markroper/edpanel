package com.scholarscore.models.query.expressions.operators;

/**
 * Enumerates the supported BinaryOperators in the warehouse reporting
 * filter expression criteria.
 * 
 * @author markroper
 *
 */
public enum BinaryOperator implements IOperator {
    OR,
    AND;
}
